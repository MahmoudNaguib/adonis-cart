@layout('main')

@section('content')
  <h3 class="mb-5 text-center">Confirm account</h3>
  @if(row.id)
    <h5>Welcome {{row.name}}</h5>
    <div class="alert alert-success" role="alert">
        Your account has been confirmed!
    </div>
  @else
    <div class="alert alert-danger" role="alert">
      Invalid confirm token
    </div>
  @endif
@endsection
